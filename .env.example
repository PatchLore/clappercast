# Server port (default: 3747). Railway injects PORT automatically.
PORT=3747

# Railway / production: set NODE_ENV=production in the dashboard Variables tab
# NODE_ENV=production

# Set to 'true' to enable verbose WebSocket logging
DEBUG=false

# Optional: ngrok tunnel for remote phone access (get token at ngrok.com)
NGROK_AUTHTOKEN=
